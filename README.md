# table-faible
Gives an ETL pipeline which utilizes a lot of data mappings some structure. It can make a pandas project less error-prone and becomes actually readable. To get started have a look at core/mapping.py and it's usage in tests/test_core.py
